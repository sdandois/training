import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  view:{
    height:80,
    position:'relative',
    padding:10,
    justifyContent:'center',
    alignItems: 'center',
    backgroundColor: '#EEE'
  }
});
